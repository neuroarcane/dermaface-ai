# DermaFace AI — dependencies (proposed; pin versions once environment is set up)

# Core ML
torch
torchvision
numpy
pandas
scikit-learn
Pillow

# Explainability (Grad-CAM)
grad-cam
# cv2 — face-presence check (preprocessing.has_face) + app helpers.
# PINNED <5 deliberately: OpenCV 5.0 moved CascadeClassifier out of `objdetect`
# into `xobjdetect` (opencv_contrib), so plain opencv-python>=5 cannot run the
# Haar face detector. Either keep this pin, or switch to opencv-contrib-python.
opencv-python<5

# App / demo
streamlit
# gradio            # alternative front-end — pick one

# Experiment tracking (pick one)
# wandb
# mlflow

# Viz / EDA
matplotlib
seaborn

# Image dedup / quality
imagehash

# Dev
pytest
black
ruff
