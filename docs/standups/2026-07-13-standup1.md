# DermaFace AI — Sprint 1, Standup 1

> **Project:** DermaFace AI (mathematics / Deep Learning course) — *not* the Dental Cavity Detector.

**Date:** 13 July 2026
**Type:** Standup (first for DermaFace)
**Attendees:** Ali, Iva, Aparna, Rolando, Varsha, Hessam, Temirlan *(all present)*

## Round-table updates

**Iva — ML Research + Evaluation** (#1, #17)
- ✅ **Backbone decision made:** confirmed **ResNet50** as the baseline — ImageNet-pretrained, reliable for transfer learning, good performance/compute balance.
- ✅ **Severity decision:** recommends **Option 1 — concept-derived proxy** (from `docs/data-strategy.md`).
- ✅ Ran the **toy test** to verify the evaluation function; committed code to GitHub.
- ❓ Asked who merges the three datasets → clarified it's **Aparna + Rolando's** task.

**Ali — UI/UX** (#2)
- ✅ Streamlit app with consent / upload / disclaimer built; no problems. **Part is done** until the real model/data arrives, then he'll update the UI.
- 🙋 Offered to **take on some of Temirlan's tasks** (Temirlan on 7, Ali on 1); emailed Hessam.

**Aparna + Rolando — Data Acquisition, Manifest, QA** (#4, #14, #15)
- 🚦 **Confirmed as the starting point** — the pipeline begins with data acquisition/prep.
- Aparna to get on it; Rolando locating the data-sources doc.
- Rolando asked about **minimum dataset size** → Hessam: download locally, run the pipeline for stats, optimize later.
- Varsha flagged: **check class balance** — 3 sources may over-represent one class.

**Varsha — MLOps** (#5, #18)
- Wrote a basic **CNN baseline**, but will switch to the **ResNet backbone (in Keras)** per Iva's decision; will run benchmarks once the dataset is ready.
- 🚧 Downstream work waits on the dataset.

**Temirlan — Evaluation & Explainability Support**
- Acknowledged he's **spread across 7 tasks** ("need to pair with everyone"). To be **partially reshuffled to Ali**.

**Hessam — Product Lead / PM**
- Confirmed pipeline order: **data acquisition first** (Aparna + Rolando).
- Cross-check: ensure **all listed data sources** are included for download.
- 🚧 **Git sync issue** — his "raw data sources" doc is stuck on an unmerged branch; will share via **OneDrive/Teams** in the interim.
- ✅ Agreed to **reshuffle some of Temirlan's tasks to Ali**.

## Decisions
1. **Backbone = ResNet50** (baseline; Varsha implements in Keras).
2. **Severity = Option 1, concept-derived proxy.**
3. **Data acquisition & dataset merge owned by Aparna + Rolando.**
4. **Temirlan overloaded → Hessam to reshuffle some tasks to Ali.**
5. Interim doc-sharing via **OneDrive/Teams** until the git branch issue is fixed.

## Action items
| Owner | Action | Ref |
|---|---|---|
| Aparna + Rolando | Start data acquisition; merge the 3 datasets; include all listed sources; check class balance; run pipeline for stats | #4, #14, #15 |
| Iva | Document ResNet50 + concept-derived-proxy decisions | #1 |
| Varsha | Write ResNet backbone (Keras); benchmark once data is ready | #5 |
| Ali | Fix the git branch sync issue after the call; absorb some of Temirlan's tasks | #21 |
| Hessam | Reshuffle Temirlan's tasks → Ali; post raw-data-sources doc to Teams | #3 |
| Temirlan | Continue paired tasks (some reassigning to Ali) | — |

## Blockers
- **Everything downstream** (Varsha's training/benchmarks, Temirlan's eval) **waits on Aparna + Rolando's dataset/manifest.**
- **Git branch sync** — Hessam's data-sources doc unmerged (Ali to fix post-call).

## Next standup
**Wednesday, same time** (5:30 / 5:45 for both projects).

---
> **Later update (2026-07-17):** the "Keras" mentions above reflect the plan on 13 July.
> The team subsequently **unified on PyTorch** (to match the rest of the stack); Varsha is
> porting her model to torchvision. See [report-draft.md](../report-draft.md) decisions log.
